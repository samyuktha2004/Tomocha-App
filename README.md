
  # TomochaApp

  This is a code bundle for TomochaApp. The original project is available at https://www.figma.com/design/uDpdgZ3nnUPJPAbSf9yigs/TomochaApp.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  